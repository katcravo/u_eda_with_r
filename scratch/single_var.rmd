---
title: "single variables"
author: "Kathleen Cravotta"
date: "November 19, 2016"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. 
```{r pseudo facebook}
pf <- read.csv("pseudo_facebook.tsv", sep = '\t')
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
library(ggplot2)
library(gridExtra)
```
```{r}
names(pf)
ggplot(aes(x = dob_day), data = pf) + 
  geom_histogram(binwidth = 1) + 
  scale_x_continuous(breaks = 1:31)
```

You can also embed plots, for example:

```{r facets, echo=FALSE}
ggplot(aes(x = dob_day), data = pf) + 
  geom_histogram(binwidth = 1) + 
  scale_x_continuous(breaks = 1:31) + 
  facet_wrap (~dob_month, ncol=3)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
ggplot(aes(x = friend_count), data = pf) + 
  geom_histogram()+
  scale_x_continuous(limits = c(0, 1000))

```

setting bins

```{r}
ggplot(aes(x = friend_count), data = pf) + 
  geom_histogram(binwidth = 25)+
  scale_x_continuous(limits = c(0, 1000), breaks = seq(0,1000,50))
```

facet
```{r}
ggplot(aes(x = friend_count), data = pf) + 
  geom_histogram(binwidth = 25)+
  scale_x_continuous(limits = c(0, 1000), breaks = seq(0,1000,50))+
  facet_wrap(~gender)
```

omit na
```{r}
ggplot(aes(x = friend_count), data = subset(pf, !is.na(gender))) + 
  geom_histogram(binwidth = 25)+
  scale_x_continuous(limits = c(0, 1000), breaks = seq(0,1000,50))+
  facet_wrap(~gender)
```
statistics by gender
```{r}
table(pf$gender)
by(pf$friend_count, pf$gender, summary)
```
The mean is higher because it is long tailed

Checking tenure
```{r}

ggplot(aes(x = tenure), data = pf) + 
   geom_histogram(binwidth = 30, color = 'black', fill = '#099DD9')
```
Checking tenure
```{r}

ggplot(aes(x = tenure/365), data = pf) + 
   geom_histogram(binwidth = .25, color = 'black', fill = '#099DD9') +
   scale_x_continuous(breaks = seq(1, 7, 1), limits = c(0, 7)) + 
   xlab('Number of years using Facebook') + 
   ylab('Number of users in sample')
```
age
```{r}
ggplot(aes(x = 2016-dob_year), data = pf) + 
   geom_histogram(binwidth = 1, color = 'black', fill = '#099DD9') +
   scale_x_continuous(breaks = seq(0, 120, 5)) + 
   xlab('Age of Users') + 
   ylab('Number of users in sample')
```
age
that generated the plot.

```{r}
summary(pf$friend_count)
summary(log10(pf$friend_count+1))
summary(sqrt(pf$friend_count))

```

setting bins

```{r}
ggplot(aes(x = log10(friend_count)), data = pf) + 
  geom_histogram()
    #binwidth = 25)+
  #cale_x_continuous(limits = c(0, 1000), breaks = seq(0,1000,50))
```
sqaure root

```{r}
ggplot(aes(x = sqrt(friend_count)), data = pf) + 
  geom_histogram()
    #binwidth = 25)+
  #cale_x_continuous(limits = c(0, 1000), breaks = seq(0,1000,50))
```
sqaure root

```{r}
p_n = ggplot(aes(x = friend_count), data = pf) + 
  geom_histogram()
p_log10 = ggplot(aes(x = log10(friend_count)), data = pf) + 
  geom_histogram()
p_sqrt = ggplot(aes(x = sqrt(friend_count)), data = pf) + 
  geom_histogram()
# arrange plots in grid
grid.arrange(p_n, p_log10, p_sqrt, ncol=1)

```
sqaure root

```{r}
p_n = ggplot(aes(x = friend_count), data = pf) + 
  geom_histogram()
p_log10 = p_n + scale_x_log10()
p_sqrt = p_n + scale_x_sqrt()
# arrange plots in grid
grid.arrange(p_n, p_log10, p_sqrt, ncol=1)

```

```{r}
ggplot(aes(x = friend_count, y = ..count../sum(..count..)), data = subset(pf, !is.na(gender))) + 
  geom_freqpoly(aes(color = gender), binwidth=10) + 
  scale_x_continuous(limits = c(0, 1000), breaks = seq(0, 1000, 50)) + 
  xlab('Friend Count') + 
  ylab('Percentage of users with that friend count')
```

frequency polygon:
```{r}

ggplot(aes(x = friend_count), data = subset(pf, !is.na(gender))) + 
  geom_freqpoly(aes(color = gender), binwidth=10) + 
  xlab('Friend Count') + 
  ylab('Percentage of users with that friend count') +
  scale_y_log10()
```
frequency polygon:
```{r}

ggplot(aes(x = www_likes), data = subset(pf, !is.na(gender))) + 
  geom_freqpoly(aes(color = gender)) + 
  scale_x_continuous()+
  scale_x_log10()
```
by stuff
```{r}
by(pf$www_likes, pf$gender, sum)
```
box plots
```{r}

ggplot(aes(x = gender, y = friend_count), data = subset(pf, !is.na(gender))) + 
  geom_boxplot() +
  coord_cartesian(ylim = c(0, 250))
```



box plots
```{r}
by(pf$friend_count,pf$gender, summary)
```
initiated friends
```{r}
by(pf$friendships_initiated ,pf$gender, summary)
```


initiated friends
```{r}
ggplot(aes(x = gender, y = friendships_initiated), data = subset(pf, !is.na(gender))) + 
  geom_boxplot() +
  coord_cartesian(ylim = c(0, 150))
```




mobile check-ins
```{r}
pf$mobile_check_in <- NA
pf$mobile_check_in <- ifelse(pf$mobile_likes > 0, 1, 0)
pf$mobile_check_in <- factor(pf$mobile_check_in)
summary(pf$mobile_check_in)
```


mobile check-ins
```{r}
mean(ifelse(pf$mobile_likes > 0, 1, 0))
sum(pf$mobile_check_in==1) / length(pf$mobile_check_in)
```












